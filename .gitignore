# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# user-specific MCP config (use mcp-config.example.ts as template)
mcp-config.ts

# caches
.eslintcache
.cache
*.tsbuildinfo
__pycache__/

# IntelliJ based IDEs
.idea

# Cursor
.cursor

# Finder (MacOS) folder config
.DS_Store
/claude-bot-standalone

# Nested git repos (accidental)
/claude-telegram-bot/

# Runtime files
.bot.pid
.work-state.json

# ML models (large binaries)
models/
.claude/settings.local.json
.claude/*.local.md

# leaked save files
.env.save
.env.backup
*.env.bak
cache/

# Maker manual extracted text (copyrighted, do not commit) — keyence / iai / oriental-motor / ...
workspace/*-manuals/**/text/
workspace/*-manuals/**/*.pdf

# Project ladder analysis scratch (customer-derived; canonical copy lives in Dropbox project _AI解析_croppy/)
workspace/*-analysis/

# Aerox watcher runtime state (regenerated)
data/aerox-watch-cache.json

# runtime notification logs
logs/
